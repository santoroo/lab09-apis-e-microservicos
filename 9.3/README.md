# Exercício 9.3 - Deploy na Nuvem (Azure App Services)

Aplicação Flask "Hello World" hospedada na Azure via App Services com CI/CD pelo GitHub Actions.

## Como rodar localmente

```bash
pip install flask
python app.py
```

## Deploy

A aplicação é deployada automaticamente na Azure via GitHub Actions a cada push na branch `master`.
